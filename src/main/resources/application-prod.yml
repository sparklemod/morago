spring:
  mail:
    host: email-smtp.ap-northeast-2.amazonaws.com
    port: 587
    username: YOUR_SES_SMTP_USERNAME
    password: YOUR_SES_SMTP_PASSWORD
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

admin:
  email: real-admin@yourdomain.com
